{% extends 'base.html.twig' %}

{% block title %}Edit Dovetailing{% endblock %}

{% block body %}
    <div class="container m-4">
        <div class="float-end">
            <a href="{{ path('app_dovetailing_index') }}">back to list</a>
        </div>
        <div class="mt-2 mb-4">
            <h2>Edit Album</h2>
            {{ include('dovetailing/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    <div class="float-end">
        {{ include('dovetailing/_delete_form.html.twig') }}
    </div>
{% endblock %}
