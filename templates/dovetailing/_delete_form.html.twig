<form method="post" action="{{ path('app_dovetailing_delete', {'id': dovetailing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dovetailing.id) }}">
    <button class="btn btn-link">Delete</button>
</form>
