{% extends 'base.html.twig' %}

{% block title %}New Image{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="float-end">
            <a class="btn btn-primary" href="{{ path('app_image_index') }}">back to list</a>
        </div>
        <div class="mt-2 mb-4">
            <h2>Upload new Image</h2>
            <div class="border border-light border-4 rounded p-2 mt-4">
                {{ include('image/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
